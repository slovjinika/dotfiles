bind-key s set status
setw -g status on
set-option -g status-style bg=red
#set -g status-right '%H:%M '
set -g status-right ''
#set-option -g status-right "#()"
#set-option -g status-right "#(python /data/data/com.termux/files/home/storage/shared/you/dec2glag/tmux-glagolitic-clock.py)"
#set-option -g status-position top
#set-option -g status-left ' '

set -g status-bg colour1
set -g window-status-current-style 'fg=colour255,bg=default'
set -g window-status-style 'fg=colour252,bg=default'

#set-option -g status-position top
#set-option -g status-style 'position=absolute,top=-2'

#set -g status-style "bg=colour235,bright"
#set -g status-left-length 100
#set -g status-right-length 100
#set -g status-left "#[fg=colour239,bg=colour252,bright] #S #[fg=colour252,bg=colour0,nobright]"
set -g status-left " #S "
#set -g status-justify centre


set -g mouse on

# Управление окнами
bind-key h split-window -h
bind-key v split-window -v
bind-key -n C-w kill-pane
#bind-key -n C-S-w kill-window

bind-key -n C-t new-window

#bind-key -n C-Tab last-window
#bind-key -n C-a last-window

#bind-key -n M-7 send-keys -l "´"

unbind-key C-b
set-option -g prefix `
bind-key ` send-prefix
bind-key ` previous-window

# -- buffers -------------------------------------------------------------------
#set -g window-status-current-style 'fg=colour245,bg=default'
#set -g window-status-style 'fg=colour235,bg=default'

bind b list-buffers     # list paste buffers
bind p paste-buffer -p  # paste from the top paste buffer
bind P choose-buffer    # choose which buffer to paste from

# monokai

set-option -g status-position top
#set-option -g status-fg color244
#set-option -g status-bg color237

set-option -g status-fg color7
set-option -g status-bg color0 #color237

bind-key -T copy-mode C-v copy-mode


# вкладки
setw -g window-status-current-style 'fg=colour7 bg=colour0'
#setw -g window-status-current-format ' #I #W #F '
setw -g window-status-current-format ' #W '

setw -g window-status-style 'fg=colour7 dim'
#setw -g window-status-format ' #I #[fg=colour7]#W #[fg=colour7]#F '
setw -g window-status-format ' #[fg=colour7]#W '


# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Other examples:
#set -g @plugin 'fabioluciano/tmux-tokyo-night'
#set -g @plugin "wabri/tmux-nord-plusplus"
#set -g @plugin 'dracula/tmux'
#set -g @plugin 'sainnhe/tmux-fzf'
#set -g @plugin 'joshmedeski/tmux-nerd-font-window-name'
#set -g @plugin 'tmux-plugins/tmux-sidebar'
#exitset -g @plugin 'abhinav/tmux-fastcopy'
#set -g @plugin 'wfxr/tmux-power'
#set -g @tmux_power_theme '#EEEEEE' # dark slate blue
#set -g @plugin 'lawabidingcactus/tmux-gruvbox-truecolor'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'


# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l

